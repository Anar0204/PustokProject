@model List<Slider>

<div style="align-items:center; justify-content:center; display:flex; ">
    <a class="btn btn-primary btn-sm mb-3" style="height:50px; width:100px" asp-area="Admin" asp-controller="Slider" asp-action="Create">
        <i class="bi bi-plus"></i> Create Slider
    </a>
</div>

<table class="table table-bordered">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Title1</th>
            <th scope="col">desc</th>
            <th scope="col">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Title1</td>
                <td>@item.desc</td>
                <td style="display:flex justify-content:space-between;">
                    <a class="btn btn-success btn-sm mb-3" style="height:30px; width:40px" asp-area="Admin" asp-controller="Slider" asp-action="Edit" asp-route-id="@item.Id">
                        <i class="bi bi-plus"></i> Edit
                    </a>
                    <a class="btn btn-warning btn-sm mb-3" style="height:30px; width:40px" asp-area="Admin" asp-controller="Slider" asp-action="Detail" asp-route-id="@item.Id">
                        <i class="bi bi-plus"></i> Detail
                    </a>
                    <form asp-area="Admin" asp-controller="Slider" asp-action="Delete" asp-route-id="@item.Id" method="post" style="display:inline;">
                        <button type="submit" class="btn btn-danger btn-sm mb-3" style="height:30px; width:70px" onclick="return confirm('Are you sure you want to delete this author?');">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </form>

                </td>
            </tr>
        }
    </tbody>
</table>